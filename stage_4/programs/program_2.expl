// reads number from input until 0 is entered and prints the sum
begin
    decl
        int a, c;
    enddecl

    read(a);
    c = a;
    while (a != 0) do
        read(a);
        c = c + a;
    endwhile;

    write("sum");
    write(c);
end
