# primitive type
u8
u64 
i64

# variale declaration
let name : type = initial_value;
let a : u32 = 10;

# function definition

proc function_name ( parameter_list ) -> (return values)

endproc

# user define type

data type_name
    a : u32,
    b : u8,
enddata

data str
    bytes: u8*,
    size: u32,
    capacity: u32,
enddata

s : str = data byte = "hello", size = 5, capacity = 100 enddata;
