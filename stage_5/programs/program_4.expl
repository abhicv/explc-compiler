//finds largest number in given thre numbers
begin
    decl
        int a, b, c;
    enddecl

    read(a);
    read(b);
    read(c);
    
    if (a < b) then
        a = b;
    endif;

    write("largest");

    if(c > a) then
        write(c);
    else
        write(a);
    endif;
end